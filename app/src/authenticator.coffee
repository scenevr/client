define [
], () ->
  class Authenticator
    auth: ->
      # todo...
      
      # div = $("<div id='authenticator' />")
      # ifame = $("<iframe src='...'></iframe>")
      # div.addClass 'a'

  Authenticator